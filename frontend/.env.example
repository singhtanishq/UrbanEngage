# Copy this file to .env (or configure in your hosting provider) and point it
# at your deployed backend, e.g. https://urban-engage-api.onrender.com
# Must be set for production builds — without it the app calls the current origin.
REACT_APP_API_URL=
